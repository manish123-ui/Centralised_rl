package com.example.demo.Controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping
public class ratelimit {

    //think about the api
    /*
    * //user and thos will be in auth service those db will be diff
    * auth_service which will be responsible for those auth token type of thing for security
    * after security it will goes to limiting service
    *
    *
    * */

}
